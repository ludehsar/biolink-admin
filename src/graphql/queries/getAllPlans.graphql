query GetAllPlans {
  getAllPlans {
    errors {
      errorCode
      field
      message
    }
    plans {
      id
      name
      monthlyPrice
      annualPrice
      enabledStatus
      visibilityStatus
    }
  }
}
