query GetAllPlans {
  getAllPlans {
    errors {
      ...ReceivedErrors
    }
    plans {
      id
      name
      monthlyPrice
      annualPrice
      enabledStatus
      visibilityStatus
    }
  }
}
