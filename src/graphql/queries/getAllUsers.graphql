query GetAllUsers($options:ConnectionArgs!) {
  getAllUsers(options: $options) {
    pageInfo {
      ...PageInfo
    }
    edges {
      node {
        id
        email
        emailVerifiedAt
        accountStatus
        language
        lastIPAddress
        country
        plan {
          name
        }
      }
      cursor
    }
    errors {
      ...ReceivedErrors
    }
  }
}
