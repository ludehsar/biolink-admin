query GetAllUsers($options:ConnectionArgs!) {
  getAllUsers(options: $options) {
    pageInfo {
      ...PageInfo
    }
    edges {
      node {
        id
        email
        lastActiveTill
        language
        lastIPAddress
        country
        plan {
          name
        }
        createdAt
      }
      cursor
    }
    errors {
      ...ReceivedErrors
    }
  }
}
