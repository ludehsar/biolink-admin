query GetLinkSettings {
  getLinkSettings {
    errors {
      ...ReceivedErrors
    }
    settings {
      branding
      enableLinkShortenerSystem
      enablePhishtank
      enableGoogleSafeBrowsing
    }
  }
}