query GetCode($codeId: String!) {
  getCode(codeId: $codeId) {
    errors {
      ...ReceivedErrors
    }
    code {
      id
      type
      code
      discount
      quantity
      expireDate
      createdAt
      updatedAt
      referrer {
        id
        email
      }
    }
  }
}
