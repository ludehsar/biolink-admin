query GetUserSummaryCounts($userId: String!) {
  getUserSummaryCounts(userId: $userId) {
    errors {
      ...ReceivedErrors
    }
    result {
      totalBiolinks
      totalShortenedLinks
      totalReferralCodes
      totalPayed
    }
  }
}
