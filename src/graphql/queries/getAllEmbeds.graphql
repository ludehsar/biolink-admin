query GetAllEmbeds($options: ConnectionArgs!) {
  getAllEmbeds(options: $options) {
    errors {
      ...ReceivedErrors
    }
    pageInfo {
      ...PageInfo
    }
    edges {
      node {
        id
        linkTitle
        linkImageUrl
        url
        shortenedUrl
        createdAt
        user {
          id
          email
        }
      }
      cursor
    }
  }
}
