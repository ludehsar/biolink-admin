query GetMainSettings {
  getMainSettings {
    errors {
      ...ReceivedErrors
    }
    settings {
      title
      defaultLanguage
      defaultTimezone
      enableEmailConfirmation
      enableNewUserRegistration
      termsAndConditionsUrl
      privacyPolicyUrl
    }
  }
}