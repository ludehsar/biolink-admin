mutation EditAdsSettings($options: AdsSettingsInput!) {
  editAdsSettings(options: $options) {
    errors {
      ...ReceivedErrors
    }
    settings {
      header
      footer
      biolinkPageHeader
      biolinkPageFooter
    }
  }
}