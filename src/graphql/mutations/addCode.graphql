mutation AddCode($options: NewCodeInput!) {
  addCode(options: $options) {
    errors {
      ...ReceivedErrors
    }
    code {
      id
      type
      code
      discount
      quantity
      expireDate
      createdAt
      updatedAt
      referrer {
        id
        email
      }
    }
  }
}
