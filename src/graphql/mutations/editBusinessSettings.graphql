mutation EditBusinessSettings($options: BusinessSettingsInput!) {
  editBusinessSettings(options: $options) {
    errors {
      ...ReceivedErrors
    }
    settings {
      enableInvoice
      name
      address
      city
      country
      zipCode
      email
      phone
      taxType
      taxId
    }
  }
}