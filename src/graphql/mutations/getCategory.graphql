query GetCategory($id: Int!) {
  getCategory(id: $id) {
    errors {
      errorCode
      field
      message
    }
    category {
      id
      categoryName
      createdAt
      updatedAt
    }
  }
}
