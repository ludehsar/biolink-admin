mutation CreateCategory($options: NewCategoryInput!) {
  createCategory(options: $options) {
    errors {
      ...ReceivedErrors
    }
    category {
      id
      categoryName
      createdAt
      updatedAt
    }
  }
}
